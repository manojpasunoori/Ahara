import type { Restaurant } from '@/lib/api';

export function RestaurantCard({restaurant}:{restaurant:Restaurant}){const meta=[restaurant.rating!==null?`${restaurant.rating.toFixed(1)} star`:null,restaurant.price_level!==null?'$'.repeat(restaurant.price_level):null,restaurant.open_now!==null?(restaurant.open_now?'Open':'Closed'):null].filter((item):item is string=>item!==null);return <article className="restaurant-card"><h3>{restaurant.name}</h3>{restaurant.categories.length>0&&<p className="restaurant-categories">{restaurant.categories.join(' · ')}</p>}<p className="restaurant-distance">{restaurant.distance_miles.toFixed(1)} mi</p>{restaurant.address&&<p className="restaurant-address">{restaurant.address}{restaurant.city?`, ${restaurant.city}`:''}</p>}{meta.length>0&&<p className="restaurant-meta">{meta.join(' · ')}</p>}{restaurant.website&&<a href={restaurant.website} target="_blank" rel="noreferrer" aria-label={`Visit ${restaurant.name} website`}>Website</a>}</article>}